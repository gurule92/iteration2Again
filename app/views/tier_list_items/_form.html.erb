<%= form_with(model: [@tier_list, @tier_list_item], local: true) do |form| %>
  <% if tier_list_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tier_list_item.errors.count, "error") %> prohibited this tier_list_item from being saved:</h2>

      <ul>
        <% tier_list_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tier_list_id %>
    <%= form.text_field :tier_list_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :tier %>
    <%= form.number_field :tier %>
  </div>

  <div class="field">
    <%= form.label :justification %>
    <%= form.text_area :justification %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <%= form.hidden_field :tier_list_id, value: @tier_list.id %>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
